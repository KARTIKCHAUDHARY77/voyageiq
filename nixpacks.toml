[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python -m gunicorn 'app:create_app(\"production\")' --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload"
